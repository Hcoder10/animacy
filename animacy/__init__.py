"""animacy — the open interaction layer for expressive robots.

Human motion in, robot motion out. One canonical human motion space
(``animacy.schema``), one ``ROBOT.md`` per body (``animacy.profile``), and a
deterministic retarget between them (``animacy.retarget``).
"""

__version__ = "0.1.0"
